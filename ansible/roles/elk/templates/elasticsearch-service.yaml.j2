apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-service
  namespace: {{ namespace }}
spec:
  ports:
  - port: {{ elasticsearch_port }}
    targetPort: {{ elasticsearch_port }}
  selector:
    app: elasticsearch
  type: NodePort

