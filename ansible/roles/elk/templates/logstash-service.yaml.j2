apiVersion: v1
kind: Service
metadata:
  name: logstash-service
  namespace: {{ namespace }}
spec:
  ports:
  - port: {{ logstash_port }}
    targetPort: {{ logstash_port }}
  selector:
    app: logstash
  type: NodePort

