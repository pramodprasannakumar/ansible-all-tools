apiVersion: v1
kind: Service
metadata:
  name: kibana-service
  namespace: {{ namespace }}
spec:
  ports:
  - port: {{ kibana_port }}
    targetPort: {{ kibana_port }}
  selector:
    app: kibana
  type: NodePort

